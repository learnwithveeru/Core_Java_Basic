# Core_Java_Basic
main basic command to add from local to my repo :
1- git init
2- git add <filename> or git add --all
3- git commit -m" my first commit"
4- git pull --rebase origin master
5- git push -u origin master
